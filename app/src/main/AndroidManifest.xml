<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kamibisa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KamiBisa">
        <activity android:name=".ui.view.activity.CreateDonationActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.view.activity.HomeActivity" />
        <activity android:name=".ui.view.activity.RegisterActivity" />
        <activity android:name=".ui.view.activity.LoginActivity" />
        <activity android:name=".ui.view.activity.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>